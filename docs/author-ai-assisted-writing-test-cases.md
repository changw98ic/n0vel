# 作者 AI 辅助创作全流程点击级测试用例

## 1. 测试边界

- 本测试集只覆盖作者端 AI 辅助创作的真实端到端链路。
- 不覆盖登录、注册、登出、权限找回、多账号切换等账号能力。
- 不测试新增大模型供应商、provider 配置页新增、删除或编辑供应商能力。
- 不允许让 AI 填写、读取、展示、复制、导出或输出任何 API Key。
- 不在提示词、正文、角色、世界观、风格、场景、审核中心或导出文件中输入真实 API Key。
- provider 只验证系统已预置 provider 在真实创作链路中可用，且只观察可用、不可用、失败恢复等状态。
- 所有 AI 生成类用例均以真实 provider 返回内容为准，不使用 mock 响应作为通过依据。

## 2. 通用前置条件

- 测试环境已启动，作者端可访问。
- 系统中已存在至少一个已预置且可用的 AI provider。
- 浏览器未登录态不影响进入作者端，或测试环境默认进入作者端。
- 测试过程中不打开开发者配置页录入 API Key。
- 测试项目名统一使用 `AI 辅助创作 E2E 测试项目`。
- 测试正文统一使用中文，不包含敏感凭据。

## 3. 执行状态字段

- `执行状态`：`未执行` / `执行中` / `已执行` / `阻塞`。
- `执行结果`：`未记录` / `通过` / `失败` / `部分通过` / `跳过`。
- `执行日期`：使用 `YYYY-MM-DD`；未执行时填写 `未记录`。
- `执行人`：记录执行者或执行机制，例如 `手工测试`、`Codex`、`QA`。
- `执行环境`：记录设备、系统、浏览器或 App 版本等可复现信息。
- `缺陷记录`：记录缺陷编号、问题摘要或 `无`。

## 4. 点击级测试用例

### TC-001 项目列表进入与新建项目

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 项目列表、新建项目、无登录边界 |
| 前置条件 | 打开作者端首页或项目列表页 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击左侧或顶部 `项目` 入口，预期触发项目列表导航事件。<br>2. 页面进入项目列表后，点击 `新建项目` 按钮，预期打开新建项目弹窗或新建项目页面。<br>3. 在项目名称输入框输入 `AI 辅助创作 E2E 测试项目`，预期输入框实时显示完整文本。<br>4. 点击 `创建` 按钮，预期提交创建项目事件。 |
| 页面预期 | 项目列表可见；新建入口可点击；创建后列表中出现项目卡片或自动进入项目工作台。 |
| 状态/数据预期 | 项目被持久化；项目标题为输入值；默认章节或正文区域可初始化。 |
| 失败判定 | 出现登录拦截；项目名称未保存；点击创建无响应；创建后找不到项目；页面报错或空白。 |

### TC-002 项目列表搜索与打开工作台

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 项目列表、工作台进入 |
| 前置条件 | 项目 `AI 辅助创作 E2E 测试项目` 已存在 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击项目列表搜索框，预期获得焦点。<br>2. 输入 `AI 辅助创作`，预期列表按关键词过滤。<br>3. 点击目标项目卡片或 `进入工作台` 按钮，预期触发打开项目事件。 |
| 页面预期 | 页面切换到创作工作台；工作台展示项目标题、正文编辑区、AI 工具入口。 |
| 状态/数据预期 | 当前项目上下文为目标项目；后续正文、角色、世界观等数据保存到该项目下。 |
| 失败判定 | 搜索无过滤；点击项目进入错误项目；工作台缺少正文编辑入口。 |

### TC-003 工作台正文输入与本地保存状态

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 工作台正文输入、保存状态 |
| 前置条件 | 已进入目标项目工作台 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击正文编辑区，预期光标出现在正文区。<br>2. 输入 `雨停后的城市像一张刚刚擦亮的旧照片，林予安站在天桥上，看见远处的钟楼亮起第一盏灯。`，预期正文实时显示。<br>3. 点击页面空白处或等待自动保存，预期保存状态从 `未保存` 或 `保存中` 变为 `已保存`。 |
| 页面预期 | 正文内容稳定显示；保存状态提示清晰；编辑区不丢焦点异常。 |
| 状态/数据预期 | 正文被写入当前章节或当前文档；刷新页面后内容仍存在。 |
| 失败判定 | 输入内容丢失；保存状态长期停留保存中；刷新后正文为空；正文保存到错误项目。 |

### TC-004 未保存正文切页保护

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 未保存正文切页保护 |
| 前置条件 | 已进入工作台，正文编辑区可编辑 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击正文末尾并输入 `她决定在午夜前查清那封匿名信的来源。`，预期保存状态变为 `未保存` 或 `保存中`。<br>2. 在保存完成前点击 `角色`、`世界观` 或项目列表入口，预期触发离开确认或保存保护事件。<br>3. 在确认弹窗中点击 `取消`，预期停留当前正文页。<br>4. 再次点击切页入口并在弹窗中点击 `保存并离开` 或等待保存完成后离开，预期成功切页。 |
| 页面预期 | 未保存时有明确阻断或确认提示；取消后正文仍可见；确认后切页成功。 |
| 状态/数据预期 | 取消离开不丢失新增正文；确认保存后正文被持久化。 |
| 失败判定 | 未保存内容直接丢失；无提示直接切页；取消后仍离开；保存后数据缺失。 |

### TC-005 真实 AI 继续写作并采纳

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 真实 AI 继续写作、provider 可用性、采纳 |
| 前置条件 | 正文已有不少于 50 字，至少一个预置 provider 可用 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击正文末尾，预期光标位于续写插入位置。<br>2. 点击 `AI` 或 `继续写作` 按钮，预期打开 AI 继续写作面板或直接触发生成。<br>3. 如有 provider 下拉框，选择已预置 provider，预期只显示已预置项，不出现 API Key 输入要求。<br>4. 点击 `生成`，预期按钮进入禁用或 loading 状态，并显示生成中提示。<br>5. 等待真实 AI 返回续写内容，预期出现候选文本和 `采纳`、`拒绝`、`重试` 操作。<br>6. 点击 `采纳`，预期候选文本插入正文。 |
| 页面预期 | 生成中有明确反馈；返回内容为中文创作文本；采纳后候选区消失或标记已采纳。 |
| 状态/数据预期 | 正文长度增加；新增内容保存到当前章节；provider 调用状态记录为成功。 |
| 失败判定 | 要求输入 API Key；AI 输出 API Key；生成无 loading；采纳后正文未变化；内容插入到错误位置。 |

### TC-006 真实 AI 继续写作并拒绝

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 继续写作、拒绝 |
| 前置条件 | 已能生成续写候选 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击正文末尾。<br>2. 点击 `继续写作`。<br>3. 点击 `生成` 并等待候选内容返回。<br>4. 点击 `拒绝`，预期触发丢弃候选事件。 |
| 页面预期 | 候选内容被移除或折叠；正文编辑区恢复可编辑。 |
| 状态/数据预期 | 正文不新增候选内容；拒绝操作不创建正文版本变更，或仅记录操作日志。 |
| 失败判定 | 拒绝后候选仍被插入正文；正文被覆盖；按钮状态无法恢复。 |

### TC-007 真实 AI 继续写作并重试

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 继续写作、重试 |
| 前置条件 | 已生成过一次续写候选 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 触发一次 `继续写作` 并等待候选 A 返回。<br>2. 点击 `重试`，预期候选区进入重新生成 loading 状态。<br>3. 等待候选 B 返回。<br>4. 点击 `采纳`。 |
| 页面预期 | 重试期间不能重复提交多次；候选 B 替换或新增为当前候选；采纳入口可用。 |
| 状态/数据预期 | 正文只采纳候选 B；候选 A 不应同时插入；生成记录可区分重试次数。 |
| 失败判定 | 重试触发多条并发插入；候选 A 和 B 同时写入；重试后按钮卡死。 |

### TC-008 AI 重写选中文本并采纳

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | AI 重写、采纳 |
| 前置条件 | 正文中已有至少一段可选中文本 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 在正文编辑区拖拽选中一句文本，预期浮动工具条或 AI 操作入口出现。<br>2. 点击 `AI 重写`，预期打开重写面板。<br>3. 在重写要求输入框输入 `增强悬疑感，保持第三人称`，预期输入显示完整。<br>4. 点击 `生成重写`，预期进入 loading 状态。<br>5. 等待真实 AI 返回重写候选。<br>6. 点击 `采纳`，预期选中文本被候选替换。 |
| 页面预期 | 原文和候选可对照；采纳后正文显示重写结果；页面不要求 API Key。 |
| 状态/数据预期 | 仅选中范围被替换；未选中正文保持不变；保存状态更新。 |
| 失败判定 | 全文被覆盖；未选中内容被误改；重写结果为空；要求输入 API Key。 |

### TC-009 AI 重写拒绝与重试

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | AI 重写、拒绝、重试 |
| 前置条件 | 已打开重写面板并可生成候选 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 选中正文短句并点击 `AI 重写`。<br>2. 输入要求 `语言更简洁`。<br>3. 点击 `生成重写` 并等待候选 A。<br>4. 点击 `拒绝`，预期正文不变。<br>5. 再次点击 `AI 重写` 或候选区 `重试`。<br>6. 等待候选 B 返回后点击 `采纳`。 |
| 页面预期 | 拒绝后候选关闭或标记已拒绝；重试可再次返回候选。 |
| 状态/数据预期 | 拒绝不保存候选 A；采纳后只保存候选 B 替换结果。 |
| 失败判定 | 拒绝后正文变化；重试沿用旧候选；采纳后替换范围错误。 |

### TC-010 角色信息创建并参与续写上下文

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 角色、真实 AI 链路 |
| 前置条件 | 已进入目标项目 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `角色` 标签或侧边栏入口，预期进入角色管理页。<br>2. 点击 `新增角色`，预期出现角色编辑表单。<br>3. 输入角色名 `林予安`。<br>4. 在角色设定中输入 `二十七岁，前档案修复师，习惯用细节推理，不轻易信任陌生人。`。<br>5. 点击 `保存`，预期角色卡片出现。<br>6. 返回正文工作台，点击 `继续写作` 并生成。 |
| 页面预期 | 角色列表显示新增角色；正文续写内容应能自然引用或不违背角色设定。 |
| 状态/数据预期 | 角色数据保存到当前项目；AI 请求上下文包含项目角色信息或生成结果符合角色约束。 |
| 失败判定 | 角色保存失败；切回正文后角色丢失；AI 明显违背已保存角色基础设定。 |

### TC-011 世界观信息创建并参与续写上下文

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 世界观、真实 AI 链路 |
| 前置条件 | 已进入目标项目 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `世界观` 入口，预期进入世界观管理页。<br>2. 点击 `新增设定`。<br>3. 在标题输入 `雨城档案局`。<br>4. 在内容输入 `城市所有失踪案都会被归档到钟楼地下三层，普通市民不知道入口存在。`。<br>5. 点击 `保存`。<br>6. 回到正文，点击 `继续写作` 并生成。 |
| 页面预期 | 世界观条目可见；AI 续写不应与设定冲突。 |
| 状态/数据预期 | 世界观条目属于当前项目；生成请求或生成结果体现该设定。 |
| 失败判定 | 世界观保存后消失；生成内容与设定直接冲突；切换页面后设定丢失。 |

### TC-012 风格设置影响 AI 输出

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 风格、AI 继续写作 |
| 前置条件 | 正文已有基础段落 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `风格` 或 `写作风格` 入口。<br>2. 选择 `悬疑`、`克制` 或输入风格说明 `短句、冷静、强调细节`。<br>3. 点击 `保存风格`。<br>4. 返回正文点击 `继续写作`。<br>5. 点击 `生成` 并等待返回。 |
| 页面预期 | 风格设置保存后有成功提示；续写候选语言风格与设置一致。 |
| 状态/数据预期 | 风格配置持久化；AI 生成使用当前项目风格。 |
| 失败判定 | 风格无法保存；刷新后风格丢失；AI 输出明显不受风格约束。 |

### TC-013 场景卡片创建并驱动当前章节

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 场景、工作台正文、AI 链路 |
| 前置条件 | 已进入目标项目 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `场景` 入口，预期进入场景列表。<br>2. 点击 `新增场景`。<br>3. 输入场景标题 `钟楼地下三层`。<br>4. 输入场景目标 `林予安发现匿名信来自档案局内部`。<br>5. 点击 `保存`。<br>6. 点击该场景的 `开始写作` 或 `关联正文`。<br>7. 在正文页点击 `继续写作` 并生成。 |
| 页面预期 | 场景卡片可见；当前正文页显示关联场景或场景目标。 |
| 状态/数据预期 | 场景与当前章节或正文上下文关联；生成内容推进场景目标。 |
| 失败判定 | 场景无法关联正文；AI 内容偏离场景目标；关联后刷新丢失。 |

### TC-014 版本创建、查看与回滚保护

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 版本、正文状态 |
| 前置条件 | 正文已有已保存内容 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `版本` 或 `历史版本` 入口，预期打开版本面板。<br>2. 点击 `创建版本` 或 `保存快照`。<br>3. 输入版本说明 `AI 续写前版本`。<br>4. 点击 `确认`，预期新增版本记录。<br>5. 返回正文追加一段文字并保存。<br>6. 再次打开版本面板，点击刚创建版本的 `查看`。<br>7. 如存在 `恢复`，点击后应出现确认弹窗；点击 `取消`。 |
| 页面预期 | 版本列表按时间显示；查看版本不直接覆盖当前正文；取消恢复后回到当前正文。 |
| 状态/数据预期 | 快照内容与创建时一致；取消恢复不改变当前正文。 |
| 失败判定 | 查看版本直接覆盖正文；取消仍回滚；版本内容与快照时不一致。 |

### TC-015 阅读模式展示当前正文

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 阅读模式 |
| 前置条件 | 正文已有已保存内容 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `阅读模式` 按钮，预期从编辑态切换为阅读态。<br>2. 滚动页面阅读正文，预期无编辑光标和 AI 浮动工具条干扰。<br>3. 点击 `退出阅读模式` 或 `编辑`，预期返回工作台编辑态。 |
| 页面预期 | 阅读模式排版清晰；正文内容与编辑态一致；退出后编辑区可继续输入。 |
| 状态/数据预期 | 阅读模式不修改正文；退出后项目上下文保持不变。 |
| 失败判定 | 阅读模式正文缺段；阅读模式可误编辑正文；退出后内容丢失。 |

### TC-016 Provider 可用性只验证预置项

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | provider 可用性、边界 |
| 前置条件 | 系统有预置 provider |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 打开 AI 生成面板。<br>2. 点击 provider 下拉框，预期仅展示已预置 provider 或默认 provider。<br>3. 选择一个可用 provider。<br>4. 点击 `继续写作` 或 `生成`。<br>5. 等待返回成功或可解释的失败提示。 |
| 页面预期 | 不出现新增 provider 按钮作为测试入口；不出现 API Key 输入框；provider 状态可辨识。 |
| 状态/数据预期 | 成功时记录 provider 可用；失败时记录 provider 调用失败但不泄露凭据。 |
| 失败判定 | 页面要求用户输入 API Key；AI 输出疑似密钥；新增供应商配置被引导为必要步骤；provider 失败无提示。 |

### TC-017 审核中心检查 AI 生成内容

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 审核中心、AI 生成内容 |
| 前置条件 | 已采纳至少一段 AI 续写内容 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `审核中心` 入口，预期进入审核页面。<br>2. 点击 `开始审核` 或选择当前章节。<br>3. 等待审核完成，预期出现问题列表、建议或通过提示。<br>4. 点击某条建议，预期定位到对应正文片段或显示详情。<br>5. 点击 `忽略` 或 `标记已处理`，预期该项状态更新。 |
| 页面预期 | 审核中心能识别当前项目正文；建议列表可交互；状态变化可见。 |
| 状态/数据预期 | 审核结果与当前版本正文关联；处理状态持久化。 |
| 失败判定 | 审核读取不到正文；点击建议无法定位；处理后刷新状态丢失；审核过程要求 API Key。 |

### TC-018 导出当前作品

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 导出 |
| 前置条件 | 当前项目正文已保存，包含手写和 AI 采纳内容 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `导出` 按钮。<br>2. 在导出弹窗选择 `Markdown`、`TXT` 或系统支持的格式。<br>3. 点击 `确认导出`。<br>4. 等待导出完成提示或文件下载触发。 |
| 页面预期 | 导出弹窗显示项目名和格式；导出完成有明确反馈。 |
| 状态/数据预期 | 导出文件包含当前正文、章节标题和已采纳 AI 内容；不包含 API Key 或 provider 凭据。 |
| 失败判定 | 导出文件缺少正文；包含密钥或 provider 私密配置；导出按钮无响应；导出失败无提示。 |

### TC-019 AI 生成失败后的恢复

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 生成失败恢复、provider 失败 |
| 前置条件 | 可通过断网、选择不可用预置 provider、限流或测试环境故障触发一次真实失败 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 在正文页点击 `继续写作`。<br>2. 触发生成并等待失败返回。<br>3. 观察错误提示，预期说明失败原因或提示稍后重试。<br>4. 恢复网络或切换另一个已预置可用 provider。<br>5. 点击 `重试`。<br>6. 等待生成成功后点击 `采纳`。 |
| 页面预期 | 失败时 loading 结束；正文仍可编辑；重试入口可用；成功后可采纳。 |
| 状态/数据预期 | 失败不会写入空内容或半截内容；恢复后成功内容正常保存。 |
| 失败判定 | 失败后按钮永久禁用；正文被清空；错误提示泄露凭据；恢复后无法再次生成。 |

### TC-020 生成中切页保护与返回

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 生成中切页 |
| 前置条件 | 正文页可触发 AI 生成 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击 `继续写作` 并点击 `生成`。<br>2. 在生成 loading 期间点击 `角色`、`世界观` 或 `项目列表`。<br>3. 如果系统设计为阻止离开，预期出现 `生成中` 提示或确认弹窗；点击 `留在当前页`。<br>4. 再次触发切页并选择 `离开`，或等待生成完成后切页。<br>5. 返回正文页查看生成结果状态。 |
| 页面预期 | 生成中状态明确；切页行为有保护或后台任务状态可追踪；返回后不会出现重复候选。 |
| 状态/数据预期 | 若留在当前页，生成任务继续并返回候选；若确认离开，任务取消或后台完成状态明确；不会自动采纳。 |
| 失败判定 | 切页导致页面崩溃；生成结果丢失且无状态；后台完成后重复插入正文；离开后自动采纳。 |

### TC-021 采纳 AI 内容后版本与审核联动

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 采纳、版本、审核中心 |
| 前置条件 | 已有 AI 续写候选 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 点击候选内容的 `采纳`。<br>2. 等待正文保存完成。<br>3. 点击 `版本`，预期可看到自动版本、保存记录或当前变更时间更新。<br>4. 点击 `审核中心`。<br>5. 对当前章节发起审核。 |
| 页面预期 | 采纳内容在正文、版本视图、审核中心引用中保持一致。 |
| 状态/数据预期 | 采纳后的正文是审核输入；版本记录不引用未采纳候选。 |
| 失败判定 | 审核中心仍读取采纳前正文；版本记录包含拒绝候选；保存时间不更新。 |

### TC-022 项目切换不串数据

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 项目列表、工作台、数据隔离 |
| 前置条件 | 至少存在两个项目 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 在项目列表点击项目 A。<br>2. 在项目 A 正文输入唯一文本 `项目 A 专属句子` 并保存。<br>3. 点击项目列表返回。<br>4. 点击项目 B。<br>5. 查看项目 B 正文、角色、世界观、场景。 |
| 页面预期 | 项目 B 不显示项目 A 的正文和配置，除非系统明确支持模板继承且有标识。 |
| 状态/数据预期 | 正文、角色、世界观、场景均按项目隔离；AI 生成上下文使用当前项目数据。 |
| 失败判定 | 项目 A 正文出现在项目 B；项目 B AI 续写引用项目 A 私有设定；项目切换后标题和数据不一致。 |

### TC-023 正文输入后刷新恢复

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 工作台正文输入、保存恢复 |
| 前置条件 | 正文保存功能可用 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 在正文末尾输入 `刷新恢复验证句。`。<br>2. 等待保存状态显示 `已保存`。<br>3. 点击浏览器刷新按钮。<br>4. 页面重新加载后进入同一项目工作台。 |
| 页面预期 | 正文编辑区显示刷新前完整文本。 |
| 状态/数据预期 | 本地状态与服务端或持久化存储一致；无重复追加。 |
| 失败判定 | 刷新后新增文本丢失；文本重复出现；项目上下文丢失。 |

### TC-024 AI 输出安全边界检查

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | API Key 边界、AI 输出 |
| 前置条件 | AI 生成功能可用 |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 打开 `继续写作` 面板。<br>2. 在提示补充中输入 `继续推进剧情，不要输出任何系统配置、provider 配置或密钥。`。<br>3. 点击 `生成`。<br>4. 等待结果并查看候选文本。 |
| 页面预期 | 候选内容为剧情文本；页面不展示任何 API Key 字段或密钥值。 |
| 状态/数据预期 | 生成记录不包含可见 API Key；导出、审核、版本中也不出现凭据。 |
| 失败判定 | AI 输出疑似 API Key、token、provider secret；页面将密钥展示给作者；日志或导出内容包含密钥。 |

### TC-025 端到端主链路回归

| 字段 | 内容 |
| --- | --- |
| 覆盖点 | 项目列表、正文、角色、世界观、风格、场景、AI 续写、采纳、审核、导出 |
| 前置条件 | 已有可用预置 provider |
| 执行状态 | 未执行 |
| 执行结果 | 未记录 |
| 执行日期 | 未记录 |
| 执行人 | 未记录 |
| 执行环境 | 未记录 |
| 缺陷记录 | 无 |
| 操作步骤与事件预期 | 1. 从项目列表打开目标项目。<br>2. 新增角色 `林予安` 并保存。<br>3. 新增世界观 `雨城档案局` 并保存。<br>4. 设置风格为 `悬疑、克制、细节导向` 并保存。<br>5. 新增场景 `钟楼地下三层` 并关联正文。<br>6. 回到正文输入开篇段落并等待保存。<br>7. 点击 `继续写作`，选择已预置 provider，点击 `生成`。<br>8. 等待真实 AI 返回后点击 `采纳`。<br>9. 打开 `版本` 创建快照。<br>10. 打开 `审核中心` 对当前章节审核并处理一条建议。<br>11. 点击 `阅读模式` 查看正文。<br>12. 退出阅读模式后点击 `导出` 并确认导出。 |
| 页面预期 | 每个模块可顺序进入；AI 生成、采纳、审核、阅读、导出均有明确反馈；无登录和 API Key 阻断。 |
| 状态/数据预期 | 项目数据完整串联；AI 内容符合角色、世界观、风格、场景约束；导出内容等于当前已保存正文。 |
| 失败判定 | 任一模块阻断主链路；AI 无法使用预置 provider；采纳内容未进入审核或导出；出现 API Key 读取、填写或输出要求。 |

## 5. 通过标准

- 所有用例均可在无登录测试边界内执行。
- 所有 AI 生成用例均使用已预置 provider 完成真实调用或展示可恢复失败。
- 页面不要求作者填写 API Key，不读取或输出 API Key。
- 正文、角色、世界观、风格、场景、版本、审核、导出之间的数据一致。
- 生成失败、生成中切页、未保存正文切页均有明确保护或恢复路径。

## 6. 失败记录要求

- 记录失败用例编号、操作步骤序号、实际页面表现、实际状态/数据、浏览器控制台错误和网络请求结果。
- 若失败涉及 provider，只记录 provider 名称、状态码、错误类型和可见错误文案，不记录任何 API Key 或 secret。
- 若失败涉及导出，只保留不含凭据的导出样本或脱敏截图。
