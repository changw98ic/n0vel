part of 'scene_pipeline_models.dart';

// ---------------------------------------------------------------------------
// Character cognition models — pipeline-specific lightweight DTOs
// ---------------------------------------------------------------------------
// NOTE: CharacterBelief, RelationshipSlice, SocialPositionSlice, and
// PresentationState are defined in domain/character_cognition_models.dart
// (the canonical source).  This part file no longer declares duplicate
// versions.  scene_pipeline_models.dart imports the domain barrel so that
// all `part` files see the domain types.
// ---------------------------------------------------------------------------
