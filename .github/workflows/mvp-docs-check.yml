name: Docs Check

on:
  push:
    paths:
      - "docs/**"
      - "editor.pen"
      - "scripts/validate_docs_bundle.py"
      - "Makefile"
      - ".github/workflows/mvp-docs-check.yml"
  pull_request:
    paths:
      - "docs/**"
      - "editor.pen"
      - "scripts/validate_docs_bundle.py"
      - "Makefile"
      - ".github/workflows/mvp-docs-check.yml"

jobs:
  validate-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Run docs bundle validation
        run: make docs-check
